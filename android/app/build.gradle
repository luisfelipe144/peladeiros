plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.torneio_facil_novo"
    compileSdk 34 // 🔧 Definido um valor fixo

    defaultConfig {
        applicationId "com.example.torneio_facil_novo"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17  // 🔥 Atualizado para Java 17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"  // 🔥 Atualizado para Java 17
    }

    buildTypes {
        release {
            minifyEnabled false  // Manter como false para evitar conflitos
            shrinkResources false // 🚀 Corrige erro removendo a exigência de unused code shrinking
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source "../.."
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.0" // 🔥 Adicionado explicitamente
}